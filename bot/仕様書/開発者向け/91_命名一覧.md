カスタムID一覧（prefix 付き）
0) 共通

button:common:close（閉じる/削除）

button:common:back（戻る）

button:common:refresh（更新）

button:common:prev（前へ）

button:common:next（次へ）

button:common:confirm（確定）

button:common:cancel（キャンセル）

1) パネル設置パネル（/パネル設置パネル）
Buttons（どのパネルを送るか）

button:ps_send:panel_admin

button:ps_send:panel_driver

button:ps_send:panel_user

button:ps_send:panel_reg_driver

button:ps_send:panel_reg_user

button:ps_send:panel_user_check

button:ps_send:panel_ride_list

Select（送信先チャンネル選択を挟む場合）

select:ps_send:channel（ChannelSelectMenu / 送信先チャンネル）

button:ps_send:do_send（選んだチャンネルに送信確定）

2) 管理者パネル
Buttons

button:admin:open_settings（設定パネルへ）

button:admin:open_user_check（ユーザー確認パネルへ）

button:admin:open_ride_list（送迎一覧パネルへ）

button:admin:broadcast（全体告知など）

設定（設定パネル）

button:settings:edit（設定編集開始）

select:settings:channel_kind（どのチャンネルを設定するか）

select:settings:role_kind（どのロールを設定するか）

select:settings:set_channel（ChannelSelect）

select:settings:set_role（RoleSelect）

button:settings:save

button:settings:reset

3) 送迎者パネル（Driver）
Buttons

button:driver:register（登録開始）

button:driver:edit_profile（プロフィール編集）

button:driver:open_my_rides（自分の担当/応募状況）

button:driver:open_available_rides（募集中一覧）

button:driver:toggle_active（稼働ON/OFF）

Select（登録/編集で使う）

select:driver:area

select:driver:stop

Modals（テキスト入力が必要な項目）

modal:driver:nickname

modal:driver:note（備考/一言など）

4) 利用者パネル（User）
Buttons

button:user:register（登録開始）

button:user:edit_profile（プロフィール編集）

button:user:request_ride（送迎依頼作成）

button:user:my_requests（自分の依頼一覧）

button:user:cancel_request（依頼キャンセル ※対象選択が必要なら別ID）

Select（依頼作成フロー）

select:user:area

select:user:stop

select:user:datetime（日時を選ばせる運用なら）

button:user:open_request_modal（モーダル入力へ）

Modals

modal:user:nickname

modal:user:request_detail（依頼内容の自由記述/補足）

5) 登録パネル（送迎者登録パネル / 利用者登録パネル）
Driver 登録パネル

button:reg_driver:start

button:reg_driver:edit

button:reg_driver:delete

select:reg_driver:area

select:reg_driver:stop

modal:reg_driver:nickname

User 登録パネル

button:reg_user:start

button:reg_user:edit

button:reg_user:delete

select:reg_user:area

select:reg_user:stop

modal:reg_user:nickname

6) ユーザー確認パネル（運営者用）

button:user_check:approve

button:user_check:reject

button:user_check:view（詳細）

button:user_check:prev

button:user_check:next

modal:user_check:reject_reason（却下理由入力するなら）

7) 送迎一覧パネル
Buttons

button:ride_list:refresh

button:ride_list:prev

button:ride_list:next

button:ride_list:view（詳細）

button:ride_list:accept（送迎者が引き受け）

button:ride_list:cancel（キャンセル）

button:ride_list:complete（完了）

modal:ride_list:cancel_reason（理由入力するなら）